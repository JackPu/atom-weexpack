'use babel'
// weexpakc cerate a project
const weexpackCreate = require('weexpack-create');
import react from 'react';

export default {
  activate(state) {
    const path =
    weexpackCreate(path);
  }
};
